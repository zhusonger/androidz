ext {
    // android sdk
    minSdkVersion = 21
    targetSdkVersion = 30
    compileSdkVersion = 30
    buildToolsVersion = "30.0.2"

    // jitpack
    groupId = "com.github.zhusonger"
    base = "1.0.0"
    widget = "1.0.0"
    media = "1.0.0"
    plugin = "1.0.0"
}